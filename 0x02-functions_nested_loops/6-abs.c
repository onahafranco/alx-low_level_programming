#include "main.h"

/**
 * _abs - shows the absolute values of an int
 * @n: the int in concern
 *
 * Return: the abs int
*/

int print_last_digit(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
